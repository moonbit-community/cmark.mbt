import {
  "moonbitlang/core/cmp",
}

options(
  "pre-build": [
    {
      "command": "python3 $input $output",
      "input": [ "gen_entities.py", "entities.json" ],
      "output": "entities.mbt",
    },
  ],
)